apiVersion: v1
kind: Service
metadata:
  name: mop-applet-build
  labels:
    io.kompose.service: mop-applet-build
spec:
  ports:
  - port: 3000
    name: port
    targetPort: 3000
  selector:
    io.kompose.service: mop-applet-build
